plugins {
    id('java-conventions')
    id('java-library')
}

group 'com.github.allisson95.codeflix.application'

dependencies {
    implementation(project(':domain'))

    testImplementation(project(path: ':domain', configuration: 'testClasses'))
}
